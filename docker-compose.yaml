services:
  postgres:
    image: postgres:16
    container_name: coupons-postgres
    environment:
      POSTGRES_DB: couponManager
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"