spring:
  application:
    name: couponManager
  datasource:
    url: jdbc:postgresql://localhost:5432/couponManager
    username: user
    password: password
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
