spring:
  application:
    name: couponManager
  datasource:
    url: jdbc:postgresql://localhost:5432/couponManager
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true